services:
  - type: web
    name: shopvoice-invan-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node mcp-servers/unified-server.js
    healthCheckPath: /health
